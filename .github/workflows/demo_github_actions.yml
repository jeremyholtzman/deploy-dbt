name: Demo Workflow

on:
  push

  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: demo workflow step
        run: echo "This is a demo workflow"
